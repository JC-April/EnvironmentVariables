{
  "name": "newenv",
  "version": "1.0.0",
  "description": "new test json package file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.2"
  }
}
